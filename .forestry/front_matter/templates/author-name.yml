---
label: Nama
hide_body: true
display_field: name
fields:
- name: name
  type: text
  config:
    required: true
  label: Nama
- name: bio
  type: blocks
  label: Bio
  template_types:
  - bio
  config:
    min: 
    max: 
